name: task
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_screenutil: ^5.9.3
  provider: ^6.1.2
  equatable: ^2.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # hive generator 
  hive_generator: ^2.0.1
  # build_runner
  build_runner: ^2.4.11
flutter:
  uses-material-design: true
  assets:
    - asset/data/users.json
    - asset/data/auth.json
  fonts:
    - family: Roboto-Black
      fonts:
        - asset: asset/font/Roboto-Black.ttf